diff --git a/node_modules/react-native-draggable-flatlist/lib/module/components/CellRendererComponent.js b/node_modules/react-native-draggable-flatlist/lib/module/components/CellRendererComponent.js
index 7e32214..9121176 100644
--- a/node_modules/react-native-draggable-flatlist/lib/module/components/CellRendererComponent.js
+++ b/node_modules/react-native-draggable-flatlist/lib/module/components/CellRendererComponent.js
@@ -1,7 +1,7 @@
 function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }
 
 import React, { useEffect, useMemo, useRef } from "react";
-import { findNodeHandle } from "react-native";
+import { findNodeHandle, UIManager } from "react-native";
 import Animated, { runOnUI, useAnimatedStyle, useSharedValue } from "react-native-reanimated";
 import { useDraggableFlatListContext } from "../context/draggableFlatListContext";
 import { isWeb } from "../constants";
@@ -89,11 +89,11 @@ function CellRendererComponent(props) {
 
     const containerNode = containerRef.current;
     const viewNode = viewRef.current;
-    const nodeHandle = containerNode;
+    const containerTag = findNodeHandle(containerNode);
+    const viewTag = findNodeHandle(viewNode);
 
-    if (viewNode && nodeHandle) {
-      //@ts-ignore
-      viewNode.measureLayout(nodeHandle, onSuccess, onFail);
+    if (viewTag != null && containerTag != null) {
+      UIManager.measureLayout(viewTag, containerTag, onFail, onSuccess);
     }
   });
   const onCellLayout = useStableCallback(e => {
diff --git a/node_modules/react-native-draggable-flatlist/lib/module/components/DraggableFlatList.js b/node_modules/react-native-draggable-flatlist/lib/module/components/DraggableFlatList.js
index 80185d0..71caf41 100644
--- a/node_modules/react-native-draggable-flatlist/lib/module/components/DraggableFlatList.js
+++ b/node_modules/react-native-draggable-flatlist/lib/module/components/DraggableFlatList.js
@@ -238,10 +238,21 @@ function DraggableFlatListInner(props) {
     }
   }, [spacerIndexAnim]);
   const gestureDisabled = useSharedValue(false);
-  const panGesture = Gesture.Pan().onBegin(evt => {
+  const panGesture = Gesture.Pan().manualActivation(true).onBegin(evt => {
     gestureDisabled.value = disabled.value;
     if (gestureDisabled.value) return;
     panGestureState.value = evt.state;
+  }).onTouchesMove((_evt, state) => {
+    if (gestureDisabled.value) {
+      state.fail();
+      return;
+    }
+
+    if (activeIndexAnim.value >= 0) {
+      state.activate();
+    } else {
+      state.fail();
+    }
   }).onUpdate(evt => {
     if (gestureDisabled.value) return;
     panGestureState.value = evt.state;
diff --git a/node_modules/react-native-draggable-flatlist/lib/module/components/NestableDraggableFlatList.js b/node_modules/react-native-draggable-flatlist/lib/module/components/NestableDraggableFlatList.js
index 3bf1f43..3eae28a 100644
--- a/node_modules/react-native-draggable-flatlist/lib/module/components/NestableDraggableFlatList.js
+++ b/node_modules/react-native-draggable-flatlist/lib/module/components/NestableDraggableFlatList.js
@@ -1,7 +1,7 @@
 function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }
 
 import React, { useRef, useState } from "react";
-import { findNodeHandle, LogBox } from "react-native";
+import { findNodeHandle, LogBox, UIManager } from "react-native";
 import { useDerivedValue, useSharedValue } from "react-native-reanimated";
 import DraggableFlatList from "../components/DraggableFlatList";
 import { useSafeNestableScrollContainerContext } from "../context/nestableScrollContainerContext";
@@ -38,7 +38,8 @@ function NestableDraggableFlatListInner(props, ref) {
     let {
       containerRef
     } = _ref;
-    const nodeHandle = findNodeHandle(scrollableRef.current);
+    const scrollTag = findNodeHandle(scrollableRef.current);
+    const containerTag = findNodeHandle(containerRef.current);
 
     const onSuccess = (_x, y) => {
       listVerticalOffset.value = y;
@@ -46,10 +47,14 @@ function NestableDraggableFlatListInner(props, ref) {
 
     const onFail = () => {
       console.log("## nested draggable list measure fail");
-    }; //@ts-ignore
+    };
 
+    if (scrollTag == null || containerTag == null) {
+      onFail();
+      return;
+    }
 
-    containerRef.current.measureLayout(nodeHandle, onSuccess, onFail);
+    UIManager.measureLayout(containerTag, scrollTag, onFail, onSuccess);
   });
   const onDragBegin = useStableCallback(params => {
     var _props$onDragBegin;
diff --git a/node_modules/react-native-draggable-flatlist/src/components/CellRendererComponent.tsx b/node_modules/react-native-draggable-flatlist/src/components/CellRendererComponent.tsx
index e4e861a..bfea6f3 100644
--- a/node_modules/react-native-draggable-flatlist/src/components/CellRendererComponent.tsx
+++ b/node_modules/react-native-draggable-flatlist/src/components/CellRendererComponent.tsx
@@ -4,6 +4,7 @@ import {
   LayoutChangeEvent,
   MeasureLayoutOnSuccessCallback,
   StyleProp,
+  UIManager,
   ViewStyle,
 } from "react-native";
 import Animated, {
@@ -90,11 +91,11 @@ function CellRendererComponent<T>(props: Props<T>) {
 
     const containerNode = containerRef.current;
     const viewNode = viewRef.current;
-    const nodeHandle = containerNode;
+    const containerTag = findNodeHandle(containerNode);
+    const viewTag = findNodeHandle(viewNode);
 
-    if (viewNode && nodeHandle) {
-      //@ts-ignore
-      viewNode.measureLayout(nodeHandle, onSuccess, onFail);
+    if (viewTag != null && containerTag != null) {
+      UIManager.measureLayout(viewTag, containerTag, onFail, onSuccess);
     }
   });
 
diff --git a/node_modules/react-native-draggable-flatlist/src/components/DraggableFlatList.tsx b/node_modules/react-native-draggable-flatlist/src/components/DraggableFlatList.tsx
index 7c88afc..d2f0e87 100644
--- a/node_modules/react-native-draggable-flatlist/src/components/DraggableFlatList.tsx
+++ b/node_modules/react-native-draggable-flatlist/src/components/DraggableFlatList.tsx
@@ -273,11 +273,24 @@ function DraggableFlatListInner<T>(props: DraggableFlatListProps<T>) {
   const gestureDisabled = useSharedValue(false);
 
   const panGesture = Gesture.Pan()
+    .manualActivation(true)
     .onBegin((evt) => {
       gestureDisabled.value = disabled.value;
       if (gestureDisabled.value) return;
       panGestureState.value = evt.state;
     })
+    .onTouchesMove((_evt, state) => {
+      if (gestureDisabled.value) {
+        state.fail();
+        return;
+      }
+
+      if (activeIndexAnim.value >= 0) {
+        state.activate();
+      } else {
+        state.fail();
+      }
+    })
     .onUpdate((evt) => {
       if (gestureDisabled.value) return;
       panGestureState.value = evt.state;
diff --git a/node_modules/react-native-draggable-flatlist/src/components/NestableDraggableFlatList.tsx b/node_modules/react-native-draggable-flatlist/src/components/NestableDraggableFlatList.tsx
index 1559352..2dd0b8b 100644
--- a/node_modules/react-native-draggable-flatlist/src/components/NestableDraggableFlatList.tsx
+++ b/node_modules/react-native-draggable-flatlist/src/components/NestableDraggableFlatList.tsx
@@ -1,5 +1,5 @@
 import React, { useRef, useState } from "react";
-import { findNodeHandle, LogBox } from "react-native";
+import { findNodeHandle, LogBox, UIManager } from "react-native";
 import Animated, {
   useDerivedValue,
   useSharedValue,
@@ -48,7 +48,8 @@ function NestableDraggableFlatListInner<T>(
   });
 
   const onListContainerLayout = useStableCallback(async ({ containerRef }) => {
-    const nodeHandle = findNodeHandle(scrollableRef.current);
+    const scrollTag = findNodeHandle(scrollableRef.current);
+    const containerTag = findNodeHandle(containerRef.current);
 
     const onSuccess = (_x: number, y: number) => {
       listVerticalOffset.value = y;
@@ -56,8 +57,13 @@ function NestableDraggableFlatListInner<T>(
     const onFail = () => {
       console.log("## nested draggable list measure fail");
     };
-    //@ts-ignore
-    containerRef.current.measureLayout(nodeHandle, onSuccess, onFail);
+
+    if (scrollTag == null || containerTag == null) {
+      onFail();
+      return;
+    }
+
+    UIManager.measureLayout(containerTag, scrollTag, onFail, onSuccess);
   });
 
   const onDragBegin: DraggableFlatListProps<T>["onDragBegin"] = useStableCallback(
